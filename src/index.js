import {
  createStateStream,
  createSourceStateStream,
  createRelayStateStream,
} from './stateStream'
import connect from './connect'

export default {
  createStateStream,
  createSourceStateStream,
  createRelayStateStream,
  connect,
}
